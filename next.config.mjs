/** @type {import('next').NextConfig} */
const nextConfig = {
  // Esconde o indicador de dev do Next.js (o botão "N" no canto).
  // Aparece só em desenvolvimento — nunca em produção.
  devIndicators: false,
};

export default nextConfig;
